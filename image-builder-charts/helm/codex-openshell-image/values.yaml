build:
  baseImage: "quay.io/aipcc/base-images/agentic/codex:latest"
  toolsImage: "registry.access.redhat.com/ubi9/ubi-minimal:latest"
  # Pull secret for the base image registry (if private).
  # Create with: oc create secret docker-registry codex-pull-secret --docker-server=quay.io --docker-username=... --docker-password=...
  pullSecret: ""

output:
  imageStreamTag: "codex-openshell:latest"
