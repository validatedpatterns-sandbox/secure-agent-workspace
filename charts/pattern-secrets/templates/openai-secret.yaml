apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openai
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStoreRef.name }}
    kind: {{ .Values.secretStoreRef.kind }}
  target:
    name: openai
    template:
      type: Opaque
      data:
        api_key: {{ printf "{{ .api_key }}" | quote }}
  data:
    - secretKey: api_key
      remoteRef:
        key: {{ .Values.vaultPrefix }}/openai
        property: api_key
