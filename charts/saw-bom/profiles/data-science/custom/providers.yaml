apiVersion: saw.redhat.com/v1alpha1
kind: Providers
metadata:
  profile: data-science
spec:
  providers:
    - name: openai
      type: openai
      nemoclawProvider: custom
      credentialSecret: inference
      credentialSecretKey: api_key
      urlSecretKey: url
      modelSecretKey: model
