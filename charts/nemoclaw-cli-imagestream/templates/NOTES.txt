nemoclaw-cli-imagestream deployed.

The ImageStream and BuildConfig are ready. To trigger a build:

  make build-cli           # or: oc start-build {{ (split ":" .Values.output.imageStreamTag)._0 }} -n {{ .Release.Namespace }}

Follow build progress:

  make build-cli-follow    # or: oc logs -f bc/{{ (split ":" .Values.output.imageStreamTag)._0 }} -n {{ .Release.Namespace }}

Check the image after the build completes:

  oc get is {{ (split ":" .Values.output.imageStreamTag)._0 }} -n {{ .Release.Namespace }}
