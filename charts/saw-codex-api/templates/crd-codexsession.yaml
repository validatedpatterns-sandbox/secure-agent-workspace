apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: codexsessions.saw.redhat.com
spec:
  group: saw.redhat.com
  names:
    kind: CodexSession
    plural: codexsessions
    singular: codexsession
    shortNames: [cs]
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [name, owner]
              properties:
                name:
                  type: string
                  maxLength: 19
                  pattern: "^[a-z][a-z0-9-]*$"
                owner:
                  type: string
                oidcToken:
                  type: string
            status:
              type: object
              properties:
                phase:
                  type: string
                  enum: [Creating, Running, Stopping, Deleting, Error]
                message:
                  type: string
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Owner
          type: string
          jsonPath: .spec.owner
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
